name: commitlint_format
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  colorize: ^3.0.0
  commitlint_types: ^0.1.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
