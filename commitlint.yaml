include: ./packages/commitlint_config/lib/commitlint.yaml

rules:
  scope-enum:
    - 2
    - always
    - - cli
      - config
      - format
      - lint
      - load
      - read
      - release
      - rules
      - types